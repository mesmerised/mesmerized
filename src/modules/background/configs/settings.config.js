import { MODULE_NAME } from './constants';

export default {
    fetchFromServer: `${MODULE_NAME}.settings.photos.fetchFromServer`,
    newPhotoDuration: `${MODULE_NAME}.settings.photos.newPhotoDuration`,
};
