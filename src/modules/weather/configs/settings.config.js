import { MODULE_NAME } from './constants';

export default {
    showWeather: `${MODULE_NAME}.settings.weather.show`,
    unit: `${MODULE_NAME}.settings.weather.unit`,
    refreshInterval: `${MODULE_NAME}.settings.weather.refreshInterval`,
};
